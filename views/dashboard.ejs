<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('navbar', { user: user }) %>
    <div class="container">
        <h1>Dashboard</h1>
        <% if (isFirstTimeLogin) { %>
            <p>Welcome, <%= user.username %>! Please update your profile information.</p>
        <% } else { %>
            <p>Welcome back, <%= user.username %>!</p>
        <% } %>
    </div>
</body>
</html>
